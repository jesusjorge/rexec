import re

def returns():
    R = U.read("https://me2.grupocva.com/me/catalogo_ip/",ignoreError=True)
    R = R.decode('iso-8859-1')
    R = R.split('dolares')[1]
    R = R.split('>')[3]
    R = R.split('<')[0]
    R = ''.join(n for n in R if n in "0123456789.")
    return R
